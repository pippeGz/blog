<h1>Esta es la pagina que muestra los articulos</h1>

<% @articles.each do |article| %>
	<h1><%= link_to article.title, article %></h1>
	<div>
		<%= article.body %>
	</div>
<%end%>
<%= will_paginate @articles %>
